# Whisper model: tiny | base | small | medium | large
WHISPER_MODEL=base

# Toggle to use the LLM extractor (true/false). If false, only keyword+embedding logic is used.
USE_LLM_EXTRACTOR=false

# If using OpenAI for the extractor, set your API key here or export before running:
# OPENAI_API_KEY=sk-...

# Optional: Ollama local model name (e.g., llama3.1). If set, the app will try Ollama first.
# OLLAMA_MODEL=llama3.1
